# mPOP物理レシート設定ガイド

## 対応方針

Star Micronics公式のStarXpand SDK for iOS（`StarIO10` 2.13.0）を使用します。プリンタ型番をコードで限定せず、SDKが発見したStarプリンタから利用端末を選ぶため、mPOPのPOP10、POP10CI、POP10CBIを含む接続構成に対応できる設計です。

探索する接続方式はLAN、Bluetooth、Bluetooth Low Energy、USBです。

## 初期設定

1. mPOPの電源を入れ、iPadとの接続またはペアリングを済ませます。
2. TablePOSの「設定」を開きます。
3. 「mPOPプリンタとドロア」を開きます。
4. 「mPOPプリンタを検索」を押します。
5. 見つかったプリンタを選択します。
6. 必要なら「キャッシュドロアを開く」で動作確認します。

選択内容はその端末の`UserDefaults`へ保存します。店舗売上データではないため、業務データのバックアップ対象には含めません。

## 会計時の動作

- 現金会計完了時、プリンタ設定済みならキャッシュドロアを開きます。
- 会計完了サマリーの「物理レシートを印刷」で、会計時点の`Sale`／`SaleItem`スナップショットを印刷します。
- レシート末尾で部分カットを実行します。
- 印刷失敗後も会計データは消えず、会計履歴からデジタルレシートを再表示できます。
- 会計完了画面には手動のドロア開放ボタンもあります。

## レシート内容

- 会計日時、テーブル、支払方法
- 商品名、単価、数量、明細金額
- 小計、外税、丸め調整、合計
- 現金の預かり金、お釣り
- 会計ID
- 印刷、紙送り、部分カット

## iOS設定と配布時の注意

アプリにはBluetooth、ローカルネットワーク、Star外部アクセサリプロトコル`jp.star-m.starpro`を設定済みです。Bluetooth／USBのMFi対応機器と通信するアプリをApp Storeで配布する場合、Star Micronicsが案内するApple MFiアプリ承認手続きが別途必要です。Bluetooth Low Energyだけを使う場合は公式案内上この承認は不要です。

SDKの探索・通常印刷・ドロアAPIにはレシートデータを外部サーバーへ送る処理を追加していません。Star公式SDKのファームウェア更新APIなどには診断情報送信に関する規定があるため、今後それらを追加する場合はプライバシーポリシーも更新してください。

## 実機確認項目

実際のmPOPを接続したら、各利用接続方式で次を確認します。

- 探索結果に同じプリンタが表示される
- 日本語の商品名が文字化けしない
- 長い商品名と多数明細の改行
- 部分カット
- 現金会計時のドロア開放
- 用紙切れ、カバー開放、接続切断時のエラー表示
