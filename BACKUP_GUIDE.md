# TablePOS 手動バックアップガイド

## 1. できること

設定画面からTablePOSの重要データを1つのJSONファイルへ書き出せます。iOSの保存画面で`iCloud Drive`を選ぶと、端末故障・紛失・アプリ再インストール時に同じApple Accountからファイルを選んで復元できます。

通常の保存先は端末内のSwiftDataです。この機能は常時同期ではなく、利用者が実行する時点バックアップです。

## 2. バックアップ対象

- 店舗設定、税の端数設定
- テーブル設定
- カテゴリ
- 商品マスタ
- 未会計の注文と注文明細
- 会計履歴
- 会計時点の商品明細スナップショット
- 取消記録と訂正会計の参照関係

検索インデックス、Sudachi辞書、OCR元画像、キャッシュは再生成できるため含めません。

## 3. 書き出し方法

```text
設定
  → バックアップと復元
  → バックアップを書き出す
  → 保存先でiCloud Driveを選択
  → 保存
```

ファイル名は`TablePOS-backup-YYYYMMDD-HHMMSS.json`です。バックアップ日時ごとに別ファイルとして残すことを推奨します。

## 4. 復元方法

復元は、誤上書きを防ぐためアプリの初回設定前だけ実行できます。

```text
TablePOSへようこそ
  → バックアップから復元
  → iCloud Driveのバックアップファイルを選択
  → 作成日時と件数を確認
  → 復元する
```

アプリは復元前に形式バージョン、SHA-256チェックサム、ID重複、注文と明細、会計と明細、取消記録の参照関係を検証します。検証に失敗したファイルは保存しません。

## 5. 運用上の注意

- 会計履歴が失われないよう、営業終了後など決まったタイミングで書き出してください。
- ファイルアプリでバックアップを削除すると復元できません。
- JSONは可搬性と将来移行を優先した形式で、内容を直接編集するとチェックサムエラーになります。
- この版は自動バックアップ、複数端末同期、既存データへのマージには対応しません。
