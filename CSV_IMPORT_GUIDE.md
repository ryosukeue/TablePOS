# メニューCSV取込ガイド

## 1. できること

商品マスタへ複数の商品をCSVからまとめて登録・更新できます。CSVは端末内で解析され、外部サーバーへ送信されません。

アプリの`商品`タブで、右上の`読み込み`から`CSVから読み込む`を選択します。

```text
商品タブ
  → 読み込み
  → CSVから読み込む
  → ファイルを選択
  → 取込可能行とエラーを確認
  → 同名商品の扱いを選択
  → 取り込む
```

動作確認用ファイルは[Examples/menu_import_sample.csv](Examples/menu_import_sample.csv)にあります。

取り込んだ商品は通常の商品と同じ扱いです。取込後は商品一覧で商品をタップして、名称、価格、税、メニュー区分、カテゴリ、頻出、有効状態を編集できます。一覧左上のカテゴリ名から絞り込みでき、右上の`選択`では複数商品を選んで一括削除できます。

## 2. 最小のCSV

必須列は`name`と`price`だけです。

```csv
name,price
生ビール,600
枝豆,400
```

省略した項目には次の値が入ります。

- 税率: 10%
- 税区分: 内税
- メニュー区分: グランド
- カテゴリ: 未分類
- 頻出タイル: OFF
- 商品の有効状態: 有効

## 3. 正式な列構造

推奨ヘッダーは次の8列です。列の順番は自由で、未使用の任意列は省略できます。未知の列は無視されます。

| 列 | 必須 | 内容 | 使用できる値 | 省略時 |
| --- | --- | --- | --- | --- |
| `name` | 必須 | 商品名 | 空でない文字列 | なし |
| `price` | 必須 | 商品単価。内税なら税込、外税なら税抜 | 0以上の整数 | なし |
| `tax_rate` | 任意 | 税率 | `8`、`8%`、`10`、`10%` | `10` |
| `tax_type` | 任意 | 内税・外税 | `included` / `内税`、`excluded` / `外税` | `included` |
| `menu_type` | 任意 | メニュー種別 | `grand` / `グランド`、`limited` / `期間限定` | `grand` |
| `category` | 任意 | カテゴリ名 | 任意の文字列 | 未分類 |
| `is_frequent` | 任意 | 頻出タイルへ表示 | `true/false`、`1/0`、`はい/いいえ` | `false` |
| `is_enabled` | 任意 | 商品を利用可能にする | `true/false`、`1/0`、`はい/いいえ` | `true` |

次の日本語ヘッダーも利用できます。

| 推奨ヘッダー | 日本語ヘッダー |
| --- | --- |
| `name` | `商品名`、`メニュー名` |
| `price` | `価格`、`単価` |
| `tax_rate` | `税率` |
| `tax_type` | `税区分`、`内外税` |
| `menu_type` | `メニュー区分`、`商品区分` |
| `category` | `カテゴリ`、`カテゴリー` |
| `is_frequent` | `頻出`、`頻出商品` |
| `is_enabled` | `有効`、`有効状態` |

例:

```csv
商品名,価格,税率,税区分,メニュー区分,カテゴリ,頻出,有効
生ビール,600,10,内税,グランド,ドリンク,はい,はい
塩むすび,300,8,内税,グランド,フード,はい,はい
```

## 4. CSVの記述ルール

- 区切り文字は半角カンマです。
- 1行目をヘッダーにしてください。
- 商品名などにカンマ、改行、ダブルクォートが含まれる場合は、値全体をダブルクォートで囲みます。
- 値に含まれるダブルクォートは`""`のように2個重ねます。
- 価格は`600`、`600円`、`¥600`、`"1,200"`の形式を読み取れます。
- 空行は無視されます。
- 改行コードはLFとCRLFの両方に対応します。

```csv
name,price
"刺身盛り合わせ, 小","1,200"
"店長の""おすすめ""",980
```

引用符の処理は一般的なRFC 4180形式に合わせています。

## 5. 文字コードとファイル制限

- 対応文字コード: UTF-8、UTF-8 BOM付き、Shift_JIS
- 推奨文字コード: UTF-8
- 最大ファイルサイズ: 5MB
- 最大商品行数: 10,000行
- 拡張子: `.csv`を推奨。ファイル選択ではCSVまたはテキストファイルを選べます。

## 6. 同名商品とカテゴリ

既存の商品名と一致する行がある場合、取込画面で次のどちらかを選びます。

- `既存商品を更新`: 価格、税率、税区分、メニュー区分、カテゴリ、頻出、有効状態をCSVの内容へ更新
- `既存商品をスキップ`: 既存商品を変更せず、そのCSV行を保存しない

商品名の前後空白とUnicodeの全角・半角互換文字をそろえて比較します。CSVに記載されていない既存商品は削除・無効化されません。

カテゴリ名が既存カテゴリと一致すればそのカテゴリを使用します。存在しないカテゴリ名は、自動色を割り当てて新規作成します。カテゴリが空なら未分類になります。

同じCSV内に同名商品が複数ある場合は、2件目以降をエラー行として除外します。

## 7. エラー時の挙動

ファイル選択後、保存前に次を表示します。

- 取込可能な商品件数と最大100件のプレビュー
- エラー内容と元CSVの行番号
- 同名商品の処理方法

商品名が空、価格が不正、税率・区分などの値が未対応の場合、その行は保存対象から除外されます。他の正常な行は取り込めます。

取込可能な全商品と新規カテゴリは、最後にまとめてSwiftDataへ保存します。保存自体が失敗した場合は変更をロールバックします。

## 8. Excel / Numbersで作る場合

### Excel

1. 1行目にヘッダーを入力します。
2. 商品を1行ずつ入力します。
3. `名前を付けて保存`または`エクスポート`を選択します。
4. 可能なら`CSV UTF-8（コンマ区切り）`を選びます。

通常の`CSV（コンマ区切り）`でShift_JISになった場合も読み込めます。

### Numbers

1. 表の1行目をヘッダーにします。
2. `共有`または`書き出す`から`CSV`を選びます。
3. テキストエンコーディングはUnicode（UTF-8）を選択します。

## 9. 対象外

この機能は商品マスタの入力専用です。会計履歴・売上のCSV出力、商品画像、在庫数、売切れ状態の取込は含みません。

## 10. 開発者向け確認

CSVパーサーのサンプル・文字コード・引用符・エラー処理は次のコマンドで確認できます。

```sh
Scripts/test_menu_csv_parser.sh
```
