# デジタルレシートガイド

## 使い方

1. 「会計履歴」から対象の会計を開きます。
2. 「デジタルレシートを表示」を押します。
3. お客様がQRコードをスマートフォンで読み取ります。
4. 表示されたレシートは「PDFとして保存・印刷」または「レシートデータを保存」で保管できます。

店舗側はQRコードを使わず、「PDFを共有・保存」からAirDrop、メッセージ、メール、ファイル保存なども選べます。

## QRコードに入るデータ

QRコードには選択した1会計だけを、バージョン付きJSONとして格納します。

- 会計ID、会計日時、状態
- テーブル名、支払方法
- 商品名、単価、数量、税率、内税・外税
- 小計、税額、丸め調整、合計
- 現金の預かり金、お釣り
- 取消・訂正元/先の会計ID

商品マスタ、カテゴリ、他の会計、店舗設定は入りません。商品マスタが50件あってもQR容量には影響せず、その会計で注文された明細数と商品名の長さだけが影響します。

## プライバシーとサーバーレス構成

QRコードは次の形式です。

```text
https://ryosukeue.github.io/TablePOS/receipt/#r1.<会計データ>
```

会計データはURLの`#`以降にあります。ブラウザは`#`以降をWebサーバーへ送らないため、GitHub Pagesは静的な表示プログラムだけを配信し、レシート内容を受信・保存しません。表示ページにもAPI通信、アカウント、広告、アクセス解析はありません。

店舗側でのQR/PDF生成はオンデバイスです。お客様側は表示ページを最初に開くときだけインターネット接続が必要です。

## 容量超過時

読み取りやすさを優先し、URL全体が1,800バイトを超える場合はQRコードを生成しません。画面に理由を表示し、全明細を失わない「PDFを共有・保存」を利用できます。明細を黙って省略することはありません。

## 取消・訂正時の注意

QRコードは発行時点の会計スナップショットです。サーバー上に会計レコードを持たないため、あとから元会計を取消・訂正しても、すでにお客様が保存したQR内容は遠隔更新されません。照合が必要な場合は会計IDを使って店舗端末の会計履歴を確認します。

## 公開ファイル

- `docs/receipt/index.html`: お客様向けレシートビューア
- `docs/index.html`: GitHub Pagesの入口
- `TablePOS/Services/DigitalReceiptService.swift`: QR用データ、QR画像、PDFの生成
- `TablePOS/Views/History/DigitalReceiptView.swift`: 店舗端末の表示・共有画面
