# メニューCSV取込 要件定義

## 1. 目的

利用者が既存の表計算データから商品マスタを一括登録・更新できるようにし、商品を1件ずつ手入力する作業を減らす。

## 2. 対象範囲

- 対象データは`Product`と、必要に応じて新規作成する`ProductCategory`とする。
- CSVにない既存商品は変更・削除しない。
- 会計履歴、注文、在庫、売切れ、画像、売上CSVは対象外とする。
- ファイル読込、解析、検証、保存は端末内で完結させる。

## 3. 画面要件

1. 商品マスタ画面の`読み込み`メニューからCSV取込画面を開けること。
2. FilesのファイルピッカーからCSVまたはテキストファイルを1件選択できること。
3. 保存前にファイル名、取込可能件数、エラー件数を表示すること。
4. 正常行は商品名、価格、税情報、メニュー区分、カテゴリをプレビューできること。
5. エラーは元CSVの行番号と理由を表示すること。
6. 既存同名商品を`更新`または`スキップ`する方針を選択できること。
7. 取込処理中は二重実行を防ぎ、処理中表示を出すこと。
8. 完了後に新規、更新、スキップ、カテゴリ追加の件数を表示すること。
9. 取込後の商品は手入力商品と同じ編集画面で変更できること。
10. 商品一覧をカテゴリまたは未分類で絞り込めること。
11. 取込後の商品を含む複数商品を選択し、確認後に一括削除できること。

## 4. ファイル要件

- 区切り文字は半角カンマとする。
- 1行目をヘッダー行とする。
- UTF-8、UTF-8 BOM付き、Shift_JISを受け付ける。
- LFとCRLFを受け付ける。
- ダブルクォートで囲まれたカンマ、改行、`""`でエスケープされた引用符を受け付ける。
- 最大ファイルサイズは5MBとする。
- ヘッダーを除く最大データ行数は10,000行とする。
- 空行はデータ行として扱わない。

## 5. CSV列

| 正式名 | 必須 | 型 | 既定値 | Productへの対応 |
| --- | --- | --- | --- | --- |
| `name` | 必須 | 空でない文字列 | なし | `name` |
| `price` | 必須 | 0以上の整数円 | なし | `price` |
| `tax_rate` | 任意 | 8または10 | 10 | `taxRate` |
| `tax_type` | 任意 | included / excluded | included | `taxType` |
| `menu_type` | 任意 | grand / limited | grand | `menuType` |
| `category` | 任意 | 文字列 | 空 | `categoryID`を名前から解決 |
| `is_frequent` | 任意 | 真偽値 | false | `isFrequent` |
| `is_enabled` | 任意 | 真偽値 | true | `isEnabled` |

商品単価は、`tax_type=included`なら税込価格、`tax_type=excluded`なら税抜価格として扱う。

ヘッダーの順序は固定しない。未知の列は将来拡張用として無視する。利用可能な日本語別名と入力値の表記は[CSV_IMPORT_GUIDE.md](CSV_IMPORT_GUIDE.md)で定義する。

## 6. 検証要件

- `name`または`price`列が存在しないファイルは、ファイル全体のエラーとする。
- 商品名が空、価格が負数・小数・文字列の場合は行エラーとする。
- 税率、税区分、メニュー区分、真偽値が許容値以外の場合は行エラーとする。
- 同一CSV内の商品名は、前後空白除去とUnicode互換正規化後の値で比較する。
- 同一CSV内の2件目以降の同名商品は行エラーとする。
- 1行に複数の問題があれば、それぞれの理由を表示できること。
- 行エラーがあっても、正常行が1件以上あれば正常行だけを取込可能とする。

## 7. 既存データとの統合

### 更新を選択した場合

同名の既存商品について、名称、価格、税率、税区分、メニュー区分、カテゴリ、頻出、有効状態、更新日時をCSVの値へ更新する。

### スキップを選択した場合

同名の既存商品を変更せず、該当CSV行を保存対象から除外する。

### カテゴリ

- 空のカテゴリは`categoryID=nil`とする。
- 名前が一致するカテゴリがあれば再利用する。
- 存在しないカテゴリは、表示順とカテゴリ色を自動割当して新規作成する。

### 検索

新規・更新商品の両方で、Sudachiを含む現在の`ProductSearch`処理を通して検索インデックスを再生成する。

## 8. 保存・障害要件

- 取込対象の商品と新規カテゴリを1回の`ModelContext.save()`で保存する。
- 保存に失敗した場合は`ModelContext`をロールバックし、成功表示を出さない。
- CSV解析時点ではSwiftDataを変更しない。
- OCR取込、手入力、既存注文、過去会計のスナップショットへ副作用を与えない。

## 9. 受入条件

1. [Examples/menu_import_sample.csv](Examples/menu_import_sample.csv)を選択すると4商品が正常行として表示される。
2. `"刺身盛り合わせ, 小"`が1つの商品名として、`"1,200"`が1,200円として解析される。
3. 日本語ヘッダーのShift_JISファイルを読み込める。
4. 必須ヘッダー不足、未閉鎖の引用符、無効な税率を適切なエラーとして表示する。
5. エラー行と正常行が混在しても、正常行だけを保存できる。
6. 更新を選ぶと同名商品の属性と検索インデックスが更新される。
7. スキップを選ぶと同名商品が変更されない。
8. 未登録カテゴリが自動作成され、商品へ設定される。
9. CSVに存在しない既存商品は変更されない。
10. iPhoneとiPadの両方でファイル選択、プレビュー、取込完了まで操作できる。
